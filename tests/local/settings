(
  (next-id 1)
  (from "foo@example.com")
  (to "bar@example.com")
  (entries 
    ((id 0) (tags) (url "tests/document-root/rss2")))
  (smtp (host "127.0.0.1") (port 10025)))

;; vim: tabstop=2 shiftwidth=2 expandtab softtabstop=2 filetype=scheme
